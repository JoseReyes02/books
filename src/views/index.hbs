<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
  integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">

<link rel="stylesheet" type="text/css" href="css/normalize.css">
<link rel="stylesheet" type="text/css" href="icomoon/icomoon.css">
<link rel="stylesheet" type="text/css" href="css/vendor.css">
<link rel="stylesheet" type="text/css" href="style.css">

</head>

<body data-bs-spy="scroll" data-bs-target="#header" tabindex="0">

  {{!-- <div id="header-wrap">

    <header id="header">
      <div class="container-fluid">
        <div class="row">

          <div class="col-md-2">
            <div class="main-logo">
              <a href="index.html"><img src="images/main-logo.png" alt="logo"></a>
            </div>

          </div>

          <div class="col-md-10">



          </div>

        </div>
      </div>
    </header>

  </div><!--header-wrap--> --}}



  <section id="best-sellin" class="leaf-pattern-overlay mt-5">
    <div class="corner-pattern-overlay"></div>
    <div class="container">
      <div class="row justify-content-center">

        <div class="col-md-10">
          <div class="row">
            <div class="col-md-5">
              <figure class="products-thumb">
                <img src="{{publicacion.urlImagen}}" alt="book" class="single-image" style="height: 470px;">
              </figure>
            </div>

            <div class="col-md-7">
              <div class="product-entry">
                <h2 class="section-title divider">{{publicacion.titulo}}</h2>

                <div class="products-content">
                  <div class="author-name">By {{publicacion.escritor}}</div>
                  <h3 class="item-title">FREE BONUS</h3>
                  <p>{{publicacion.comentario}}</p>
                  {{!-- <div class="item-price">$ 45.00</div> --}}
                  <div class="btn-wrap" id="botonPrincipal">
                    {{#if user}}
                    <a href="{{publicacion.urlPdf}}" class="btn btn-primary btn-lg" id="iniciarDescarga" download>Get
                      the Devotional <i class="icon icon-ns-arrow-right"></i></a>
                    {{else}}
                    <a href="#" class="btn btn-primary btn-lg" onclick="abrirModalLogin()">Get the
                      Devotional <i class="icon icon-ns-arrow-right"></i></a>
                    {{/if}}
                    <div id="segundoBoton"></div>
                  </div>
                </div>

              </div>
            </div>

          </div>
          <!-- / row -->

        </div>

      </div>
    </div>
  </section>



  {{#if dounload}}
  <div id="mensajeDescarga" class="mensaje-descarga">
    Iniciando descarga...
  </div>

  <script>
    document.getElementById('segundoBoton').innerHTML = `
          <a href="{{publicacion.urlPdf}}" class="btn btn-primary btn-lg" id="iniciarDescarga" download hidden>Get the Devotional <i
            class="icon icon-ns-arrow-right"></i></a>
     `
    document.getElementById('botonPrincipal').innerHTML = ''
    document.getElementById('botonPrincipal').innerHTML = `
                         <a href="{{publicacion.urlPdf}}" class="btn btn-primary btn-lg" id="iniciarDescarga" download>Get
                      the Devotional <i class="icon icon-ns-arrow-right"></i></a>
     `

    setTimeout(() => {
      // Redirige a la URL del archivo PDF
      document.getElementById('iniciarDescarga').click()
      document.getElementById('mensajeDescarga').style.display = 'none';
    }, 1000); // Espera 1 segundo antes de iniciar descarga


  </script>
  {{/if}}



  <style>
    .mensaje-descarga {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background-color: #17a2b8;
      color: white;
      padding: 20px 30px;
      font-size: 20px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
      z-index: 9999;
      text-align: center;
      animation: fadeIn 0.5s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }
  </style>

  <!-- Modal -->
  <div id="modalLogin" class="modal">
    <div class="modal-contenido">
      <span class="cerrar" onclick="cerrarModalLogin()">&times;</span>
      <h3>Iniciar Sesión</h3>
      <div class="container">
        <div class="row justify-content-center">
          <div class="card shadow-sm">
            <div class="card-body">
              <form action="/users/signin" method="POST">
                <div class="mb-3">
                  <label for="email" class="form-label">Correo electrónico</label>
                  <input type="email" class="form-control" id="email" name="email" placeholder="correo@ejemplo.com"
                    required>
                </div>
                <div class="mb-3">
                  <label for="password" class="form-label">Contraseña</label>
                  <input type="password" class="form-control" id="password" name="password" placeholder="••••••••"
                    required>
                </div>
                <div class="d-grid">
                  <button type="submit" class="btn btn-primary btn-sm">Ingresar</button>
                </div>
              </form>
              <div class="text-center my-3">
                <span class="text-muted">o</span>
              </div>
              <div class="d-grid">
                <a href="/auth/google" class="btn btn-secondary">
                  <img src="https://www.gstatic.com/firebasejs/ui/2.0.0/images/auth/google.svg" alt="Google"
                    style="width:20px; margin-right:8px;">
                  Iniciar sesión con Google
                </a>
              </div>
            </div>
          </div>

        </div>
      </div>


    </div>
  </div>



  <style>
    /* Fondo del modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
    }

    /* Contenedor del modal */
    .modal-contenido {
      background-color: #fff;
      margin: 3% auto;
      padding: 20px;
      border-radius: 10px;
      width: 90%;
      max-width: 400px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
      animation: fadeIn 0.3s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: scale(0.9);
      }

      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    /* Botón de cerrar */
    .cerrar {
      float: right;
      font-size: 24px;
      cursor: pointer;
      color: #888;
    }

    .cerrar:hover {
      color: #000;
    }
  </style>



  <script>
    function abrirModalLogin() {
      document.getElementById('modalLogin').style.display = 'block';
    }

    function cerrarModalLogin() {
      document.getElementById('modalLogin').style.display = 'none';
    }

    // Cerrar si hace clic fuera del modal
    window.onclick = function (event) {
      const modal = document.getElementById('modalLogin');
      if (event.target == modal) {
        cerrarModalLogin();
      }
    }

    // Enviar login con fetch
    document.getElementById('formLoginCustom').addEventListener('submit', async function (e) {
      e.preventDefault();

      const email = document.getElementById('email').value;
      const password = document.getElementById('password').value;

      try {
        const res = await fetch('/login', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ email, password })
        });

        const data = await res.json();

        if (data.success) {
          cerrarModalLogin();
          window.location.href = '/'; // O redirige a donde quieras
        } else {
          alert(data.message || 'Credenciales incorrectas');
        }

      } catch (error) {
        console.error('Error al iniciar sesión:', error);
      }
    });
  </script>